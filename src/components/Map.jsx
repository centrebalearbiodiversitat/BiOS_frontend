import React from "react";

export default function Map({className}) {
	return (
		<div className={`w-full bg-blue-400 ${className} flex justify-center align-middle`}>
			<p className="m-auto text-3xl">Map</p>
		</div>
	);
}