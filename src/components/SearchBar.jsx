
import React from "react";
import {Card, CardHeader, CardBody, CardFooter, Image, Button} from "@nextui-org/react";

export default function SearchBar({upperTitle, title, src, height = 300}) {
	return (
		<div className={`min-h-[${height}px] min-h bg-blue-400 border-1 hover:bg-blue-600`}>

		</div>
	);
}